{
  "ignoreCommand": "if [ \"$VERCEL_ENV\" = 'preview' ] && [ -z \"$VERCEL_GIT_PULL_REQUEST_ID\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != 'main' ]; then echo 'Skipping orphan preview build (no PR)'; exit 1; fi"
}
